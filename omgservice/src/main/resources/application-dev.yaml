quarkus:
  keycloak:
    policy-enforcer:
      paths:
        - path: /q
          enforcement-mode: DISABLED
    devservices:
      port: 8000
      realm-path: omgservers-realm.json
  oidc:
    credentials:
      secret: omgservice
